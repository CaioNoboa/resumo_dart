/*
  Composição é construir um objeto com outros objetos, dessa forma, construimos objetos mais complexos a partir de objetos mais simples
*/

class Cliente {
  late String nome;
  late String cpf;

  Cliente({this.nome = '', this.cpf = ''}); // parâmtros nomeados
}