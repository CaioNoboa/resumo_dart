// Criação de variáveis

  // print() - imprime algo na tela

  // Dart é fortemente tipado, variáveis precisam ser identificadas quanto ao tipo

main() {
  
  // Criando uma variável:
  int a = 2; // variável número inteiro
  double b = 3.1419; // double é ponto flutuante, real, aceita casas decimais
  b = 3.1415;
  print(a);
  print(a + 2 * 4);
  print(b);
}